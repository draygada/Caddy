"""Strafe Forge search lane: a bounded proposer over an owned candidate pool.

The claim a judge can hear (research brief 2026-09-05 §1, verifier-corrected): "No part-search, BOM or
component-intelligence tool we found computes or filters parts by export-control jurisdiction or
classification at the part level. Digi-Key's API returns a manufacturer-declared ECCN as a static per-part
data field, but nowhere is it a searchable or filterable parameter; the closest thing to a filter,
Thomasnet's 'ITAR Registered' checkbox, is a self-declared company-level DDTC-registration flag, not a
per-part determination." Never say "nobody does AI part search": Wizerr, PartGenie and Flux's copilot do.

The agent proposes. The rule decides. The log proves it.
"""
